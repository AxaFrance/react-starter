import Home from 'Home';

const routes = [
  {
    name: 'Home',
    path: '/',
    component: Home,
    titlebar: {
      title: 'Accueil',
      subtitle: '',
    },
  },
];

export default routes;
